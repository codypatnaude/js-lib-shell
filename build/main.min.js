(function () {
  'use strict';

  function greeter(person) {
      return "Hello " + person;
  }

  var user = "Jane User";
  [1, 2, 3].map(function (elem) {
    return console.log(elem);
  });
  console.log(greeter(user));

}());
